
SET(DEPEND_LIB_PATH
	${PROJECT_SOURCE_DIR}/../lib
)
message("main:" ${DEPEND_LIB_PATH})

SET(DEPENDANCY_LIB 
	hello 
	ffmpeg_api
	)

LINK_DIRECTORIES(${DEPEND_LIB_PATH})

ADD_EXECUTABLE(FFmpegDemo main.cpp)

TARGET_LINK_LIBRARIES(FFmpegDemo ${DEPENDANCY_LIB})